#ifndef reNoise_h__
#define reNoise_h__

#include "reTypes.h"

glm::vec4 calculateVertex(glm::vec4 pos);
glm::vec3 calculateVertex(glm::vec3 pos);
glm::vec3 getNormal(glm::vec4 wpos);

#endif // reNoise_h__
